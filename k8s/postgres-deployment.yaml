apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: glaux-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          env:
            - name: POSTGRES_USER
              value: "glaux"
            - name: POSTGRES_PASSWORD
              value: "glaux_secret_password"
            - name: POSTGRES_DB
              value: "glaux_shortlist"
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: glaux-system
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
